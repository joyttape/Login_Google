# Configurações de Email/Webhook para receber dados capturados

# Email para receber notificações (opcional)
NOTIFICATION_EMAIL=vitorpeter9@gmail.com

# URL do Webhook (Discord, Slack, etc.) para receber notificações
WEBHOOK_URL=https://discord.com/api/webhooks/SEU_WEBHOOK_ID/SEU_WEBHOOK_TOKEN

# Configurações SMTP (se usar email direto)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=vitorpeter9@gmail.com
SMTP_PASS=sua-senha-de-app

# Configurações de Segurança
NODE_ENV=production
ALLOWED_ORIGINS=https://seu-dominio.vercel.app

# Configurações do EmailJS (alternativa)
EMAILJS_SERVICE_ID=seu_service_id
EMAILJS_TEMPLATE_ID=seu_template_id
EMAILJS_PUBLIC_KEY=sua_public_key